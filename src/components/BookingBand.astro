---
interface Props {
  salon: {
    phone: string;
  };
  booking: {
    provider: string;
    url: string;
    embedUrl: string;
    mode: "link" | "embed";
  };
  title?: string;
  text?: string;
}

const {
  salon,
  booking,
  title = "Bereit fuer deinen naechsten Termin?",
  text = "Buche online oder ruf direkt im Salon an. In einem echten Kundenprojekt fuehrt der Button zum gewaehlten Booking-System.",
} = Astro.props;
---

<section class="booking-band">
  <div class="container booking-inner">
    <div>
      <p class="eyebrow">Termin</p>
      <h2>{title}</h2>
      <p>{text}</p>
    </div>
    <div class="booking-actions">
      <a class="btn btn-primary" href="/termin/">Termin buchen</a>
      <a class="btn btn-secondary" href={"tel:" + salon.phone.replaceAll(" ", "")}>{salon.phone}</a>
    </div>
    {
      booking.mode === "embed" && booking.embedUrl ? (
        <iframe
          class="booking-frame"
          src={booking.embedUrl}
          title={booking.provider + " Terminbuchung"}
          loading="lazy"
        />
      ) : null
    }
  </div>
</section>

<style>
  .booking-band {
    background: #efe7db;
    padding: 46px 0;
  }

  .booking-inner {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 24px;
    align-items: center;
  }

  h2 {
    max-width: 680px;
    margin: 0;
    font-size: clamp(1.8rem, 3vw, 3rem);
    line-height: 1;
  }

  p {
    max-width: 650px;
    color: var(--muted);
    line-height: 1.6;
  }

  .booking-actions {
    display: flex;
    flex-shrink: 0;
    flex-wrap: wrap;
    gap: 12px;
  }

  .booking-frame {
    grid-column: 1 / -1;
    width: 100%;
    min-height: 620px;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: #fff;
  }

  @media (max-width: 820px) {
    .booking-inner {
      grid-template-columns: 1fr;
    }
  }
</style>
