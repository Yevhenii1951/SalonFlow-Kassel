---
import "../styles/global.css";
import {
  getLocalBusinessSchema,
  getSalon,
  navItems,
} from "../lib/cms";

interface Props {
  title: string;
  description: string;
  canonical?: string;
}

const { title, description, canonical = Astro.url.pathname } = Astro.props;
const salon = await getSalon();
const schema = getLocalBusinessSchema(salon, Astro.site?.origin ?? "https://salonflow-kassel.example");
const pageTitle = `${title} | ${salon.name}`;
const canonicalUrl = new URL(canonical, Astro.site).toString();
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cabin:wght@400;600;700&family=Playfair+Display:wght@500;600;700&display=swap"
      rel="stylesheet"
    />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} />
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href="/" aria-label={`${salon.name} Startseite`}>
        <span class="brand-mark">SF</span>
        <span>{salon.name}</span>
      </a>
      <nav aria-label="Hauptnavigation">
        {
          navItems.map((item) => (
            <a href={item.href} class:list={{ active: Astro.url.pathname === item.href }}>
              {item.label}
            </a>
          ))
        }
      </nav>
      <a class="btn btn-primary header-cta" href="/termin/">Termin buchen</a>
    </header>
    <main>
      <slot />
    </main>
    <a class="btn btn-primary mobile-sticky-cta" href="/termin/">Termin buchen</a>
    <footer class="site-footer">
      <div class="container footer-grid">
        <div>
          <strong>{salon.name}</strong>
          <p>{salon.tagline}</p>
        </div>
        <div>
          <strong>Kontakt</strong>
          <p>
            {salon.address.street}<br />
            {salon.address.postalCode} {salon.address.city}<br />
            <a href={`tel:${salon.phone.replaceAll(" ", "")}`}>{salon.phone}</a>
          </p>
        </div>
        <div>
          <strong>Rechtliches</strong>
          <p><a href="/impressum/">Impressum</a><br /><a href="/datenschutz/">Datenschutz</a><br /><a href="/agb/">AGB</a></p>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 30;
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 24px;
    align-items: center;
    border-bottom: 1px solid var(--dark-line);
    background: rgba(18, 16, 13, 0.92);
    padding: 14px 24px;
    color: var(--ink-on-dark);
    backdrop-filter: blur(14px);
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-weight: 700;
    text-decoration: none;
    white-space: nowrap;
  }

  .brand-mark {
    display: inline-grid;
    width: 38px;
    height: 38px;
    place-items: center;
    border-radius: 50%;
    background: var(--accent);
    color: #1a140b;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 0.82rem;
  }

  nav {
    display: flex;
    justify-content: center;
    gap: 18px;
    color: var(--muted-on-dark);
    font-size: 0.95rem;
  }

  nav a {
    text-decoration: none;
  }

  nav a:hover,
  nav a.active {
    color: var(--ink-on-dark);
  }

  .site-footer {
    border-top: 1px solid var(--dark-line);
    background: var(--dark);
    color: var(--ink-on-dark);
    padding: 44px 0;
  }

  .site-footer p,
  .site-footer a {
    color: var(--muted-on-dark);
  }

  .site-footer strong {
    color: var(--ink-on-dark);
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 28px;
  }

  @media (max-width: 920px) {
    .site-header {
      grid-template-columns: 1fr auto;
    }

    nav {
      grid-column: 1 / -1;
      justify-content: flex-start;
      overflow-x: auto;
      padding-bottom: 2px;
    }
  }

  @media (max-width: 620px) {
    .header-cta {
      display: none;
    }

    .footer-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
